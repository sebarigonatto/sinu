@model IEnumerable<SINU.Models.Delegacion_EstablecExamen>

@{
    ViewBag.Title = "Index";
    //Layout = "~/Views/Shared/_MenuPerfilAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EstablecimientoRindeExamen.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OficinasYDelegaciones.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EstablecimientoRindeExamen.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OficinasYDelegaciones.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idDelegacion_EstablecExamen }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idDelegacion_EstablecExamen }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.idDelegacion_EstablecExamen })
        </td>
    </tr>
}

</table>
